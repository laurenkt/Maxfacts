<!DOCTYPE html>
<html>
	<head>
		<title>{{title}}</title>
		<link rel="stylesheet" href="/css/style.css" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
	</head>
	<body>
		<header>
			<div>
				<h1><a href="/">MaxFacts</a></h1>
				<form action="/search" method="GET">
					<input type="search" name="query" placeholder="Search">
				</form>
			</div>
			<nav>
				<p>
					<a href="/">Home</a>
					{{#each breadcrumbs}}
					» <a href="/{{uri}}">{{title}}</a>
					{{/each}}
				</p>
			</nav>
		</header>
		<section class="{{classes}}">
			{{{body}}}
		</section>
		<footer>
			<p>
				<a href="{{path}}/feedback">Contact us</a>
				&mdash; Copyright &copy;
				&mdash; Disclaimers
				{{#if edit_uri}}&mdash; <a href="{{edit_uri}}">Dashboard: edit this page</a>{{/if}}
			</p>
		</footer>
	</body>
</html>
