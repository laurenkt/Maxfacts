{{define "content"}}
<main>
	<h2>{{.Title}}</h2>
	{{if .Body}}
		{{shift_headers 2 .Body}}
	{{end}}
	
	<div class="alphabetical">
		{{range $letter, $items := .Alphabetical}}
			{{if $items}}
			<div class="alphabetical--item">
				<h3>{{$letter}}</h3>
				<ul>
					{{range $items}}
					<li><a href="/{{.URI}}">{{.Title}}</a></li>
					{{end}}
				</ul>
			</div>
			{{end}}
		{{end}}
	</div>
	
	{{template "next-section" .}}
</main>
{{end}}